<% unless @post.nil? %>
  $('#post_<%= @post.id %>_tags').html('<%= escape_javascript(render "tags/tags", :tags => @post.tags, :show_untag_all => false, :show_untag => true, :post => @post ) %>');
<% end %>
