<% unless @post.nil? %>
  $('#tags_<%= @post.id %>').html('<%= escape_javascript(render "tags/tags", :tags => @post.tags, :show_untag_all => false, :show_untag => true, :post => @post ) %>');
<% end %>
