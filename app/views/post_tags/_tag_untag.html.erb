<div class="tag_untag">
  <% if post_tag.user_id == current_user.id %>
    <%= link_to "×", post_tag, :method => :delete, :remote => true %>
  <% else %>
    <%= link_to "+", post_tags_path(:post_id => post_tag.post.id, :tag_id => post_tag.tag.id), :method => :post, :remote => true %>
  <% end %>
</div>
