<%= form_for current_user.user_tags.find_by_tag_id(tag), :html => { :method => :delete }, :remote => true do |f| %>
  <%= check_box_tag "checkbox_#{tag.id}", tag.id, false, :onclick => "$(this).parent('form').submit();" %>
<% end %>
