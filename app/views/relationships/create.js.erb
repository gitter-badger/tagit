$("follow_form_<%= @user.username %>").update("<%= escape_javascript(render 'relationships/unfollow', :user => @user) %>")
$("following_<%= current_user.username %>").update("<%= %(#{current_user.following.count} #{t(:_following)}) %>")
$$(".followers_<%= @user.username %>").each(function(element) { element.update("<%= pluralize(@user.followers.count, t(:_follower)) %>"); });
